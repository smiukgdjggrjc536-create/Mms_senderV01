# AI-DEPLOY ACCESS GUIDE — Vercel + Netlify টোকেন তৈরি করার সম্পূর্ণ গাইড

> **আপনার কাজ শুধু ২টা টোকেন তৈরি করে আমাকে দেওয়া।** এরপর ডিপ্লয় আমি নিজে করবো।
> ফোনের Chrome ব্রাউজার দিয়েই সব কাজ হবে। কোনো কম্পিউটার বা কোনো সফটওয়্যার লাগবে না।

---

## আপনাকে যা যা করতে হবে (সারাংশ)

| ক্রম | কাজ | কতক্ষণ লাগবে |
|------|------|------------|
| ১ | Vercel টোকেন তৈরি করা | ২ মিনিট |
| ২ | Netlify অ্যাকাউন্ট তৈরি করা | ৩ মিনিট |
| ৩ | Netlify টোকেন তৈরি করা | ২ মিনিট |

**মোট সময়: ৭ মিনিট**

তারপর আপনি শুধু ২টা টোকেন আমাকে মেসেজ করে দিবেন। আমি বাকি সব করবো।

---

# অংশ ১: Vercel Access Token তৈরি করা

> Vercel অ্যাকাউন্ট আপনার আগে থেকেই তৈরি আছে এবং ডিপ্লয়ও করা আছে।
> আপনাকে শুধু একটা Access Token তৈরি করতে হবে।

## ধাপ ১: Vercel টোকেন পেজে যান

১. ফোনের **Chrome ব্রাউজার** খুলুন।

২. ঠিকানা বারে এই লিংকটা লিখুন বা কপি-পেস্ট করে এন্টার দিন:

```
https://vercel.com/account/tokens
```

৩. যদি লগইন করা না থাকে, তাহলে Vercel লগইন পেজ আসবে। আপনার ইমেইল আর পাসওয়ার্ড দিয়ে লগইন করুন। (আপনি যে ইমেইল দিয়ে Vercel অ্যাকাউন্ট তৈরি করেছিলেন সেটা দিয়ে লগইন করুন।)

৪. লগইন করার পর আপনি **"Tokens"** পেজে চলে যাবেন। এই পেজের উপরে লেখা থাকবে **"Tokens"** এবং নিচে একটা **"Create Token"** বা **"Create"** বাটন থাকবে।

## ধাপ ২: নতুন টোকেন তৈরি করুন

১. **"Create"** বা **"Create Token"** বাটনে ক্লিক/ট্যাপ করুন। (এটা পেজের উপরের দিকে ডান পাশে থাকে, নীল রঙের বাটন।)

২. একটা ফর্ম আসবে। সেখানে নিচের মতো পূরণ করুন:

   - **Token Name** (টোকেনের নাম): এখানে লিখুন → `superninja-deploy`
   
   - **Scope** (কোন এক্সেস দিবেন): এখানে ড্রপডাউন থেকে **"Full Account"** সিলেক্ট করুন। (এটা খুবই গুরুত্বপূর্ণ — Full Account সিলেক্ট করতে হবে, নাহলে আমি ডিপ্লয় করতে পারবো না।)
   
   - **Expiration** (মেয়াদ): ড্রপডাউন থেকে **"90 days"** সিলেক্ট করুন। (৯০ দিন পর্যন্ত টোকেন কাজ করবে, এরপর নতুন টোকেন লাগবে। ৯০ দিন অনেক সময়, আপাতত এটাই যথেষ্ট।)

৩. নিচে **"Create"** বাটনে ট্যাপ করুন। (নীল রঙের বাটন, ফর্মের নিচে থাকবে।)

## ধাপ ৩: টোকেন কপি করুন (খুবই গুরুত্বপূর্ণ)

১. টোকেন তৈরি হওয়ার পর একটা বক্স আসবে যেখানে আপনার টোকেন লেখা থাকবে।

২. টোকেনটা **`vcp_`** দিয়ে শুরু হবে। যেমন: `vcp_abc123xyz456...`

৩. টোকেনের পাশে একটা **কপি আইকন** (দুটো ছোট চতুর্ভুজ) থাকবে। সেটায় ট্যাপ করে টোকেন কপি করুন।

   ⚠️ **সতর্কতা:** এই টোকেনটা শুধু একবারই দেখানো হবে! পেজ রিফ্রেশ করলে বা অন্য পেজে গেলে আর দেখা যাবে না। তাই এখনই কপি করে নিন।

৪. কপি করা টোকেনটা একটু নোট করে রাখুন (নোটপ্যাড বা কোথাও পেস্ট করে রাখুন)।

৫. টোকেন কপি হওয়ার পর **"Done"** বা **"Close"** বাটনে ট্যাপ করুন।

## ✅ Vercel টোকেন প্রস্তুত!

আপনার Vercel টোকেন প্রস্তুত। এটা `vcp_` দিয়ে শুরু হয়েছে। এটা আমাকে মেসেজ করে দিন।

---

# অংশ ২: Netlify অ্যাকাউন্ট তৈরি করা

> Netlify অ্যাকাউন্ট আপনার এখনো নেই। আগে অ্যাকাউন্ট তৈরি করতে হবে, তারপর টোকেন তৈরি করতে হবে।

## ধাপ ১: Netlify ওয়েবসাইটে যান

১. ফোনের **Chrome ব্রাউজার** খুলুন।

২. ঠিকানা বারে এই লিংকটা লিখুন বা কপি-পেস্ট করে এন্টার দিন:

```
https://app.netlify.com
```

৩. Netlify এর হোম পেজ / সাইন আপ পেজ আসবে।

## ধাপ ২: সাইন আপ করুন

১. পেজে কয়েকটা অপশন থাকবে সাইন আপ করার জন্য:
   - **"Continue with GitHub"** (GitHub দিয়ে সাইন আপ)
   - **"Continue with GitLab"**
   - **"Continue with Bitbucket"**
   - **"Continue with email"** (ইমেইল দিয়ে সাইন আপ)

২. **"Continue with GitHub"** বাটনে ট্যাপ করুন। (এটা সবচেয়ে সহজ, কারণ আপনার ইতিমধ্যে GitHub অ্যাকাউন্ট আছে।)

৩. GitHub লগইন পেজ আসতে পারে (যদি ব্রাউজারে লগইন না থাকে)। আপনার GitHub ইমেইল আর পাসওয়ার্ড দিয়ে লগইন করুন।

৪. GitHub একটা **"Authorize Netlify"** পেজ দেখাবে। এখানে লেখা থাকবে Netlify আপনার GitHub অ্যাকাউন্টে কী কী এক্সেস চাইছে। নিচে **"Authorize Netlify"** বা **"Authorize"** সবুজ বাটনে ট্যাপ করুন।

৫. এরপর Netlify আপনাকে তার ড্যাশবোর্ডে নিয়ে যাবে। হতে পারে একটা ছোট সেটআপ ফর্ম আসবে যেখানে নাম ইত্যাদি জানতে চাইবে — সেগুলো পূরণ করে সাবমিট করুন।

## ✅ Netlify অ্যাকাউন্ট তৈরি হয়েছে!

---

# অংশ ৩: Netlify Access Token তৈরি করা

> এখন আপনার Netlify অ্যাকাউন্ট তৈরি হয়েছে। এবার টোকেন তৈরি করতে হবে।

## ধাপ ১: Netlify Token পেজে যান

১. Netlify তে লগইন অবস্থায় আপনার Chrome ব্রাউজারে ঠিকানা বারে এই লিংকটা লিখুন বা কপি-পেস্ট করে এন্টার দিন:

```
https://app.netlify.com/user/applications#personal-access-tokens
```

   (এই লিংকটা সরাসরি আপনাকে Personal Access Tokens পেজে নিয়ে যাবে।)

২. যদি কোনো কারণে লিংকটা কাজ না করে, তাহলে এভাবে যান:
   - Netlify ড্যাশবোর্ডে আপনার **প্রোফাইল ছবি/আভাটার** (ডান দিকে উপরের কোণায়) তে ট্যাপ করুন
   - মেনু থেকে **"User settings"** বা **"Settings"** এ ট্যাপ করুন
   - বাম দিকের মেনু থেকে **"Applications"** এ ট্যাপ করুন
   - স্ক্রোল করে **"Personal access tokens"** সেকশন খুঁজে বের করুন

## ধাপ ২: নতুন টোকেন তৈরি করুন

১. **"Personal access tokens"** সেকশনে একটা **"New access token"** বাটন থাকবে। সেটায় ট্যাপ করুন।

২. একটা ছোট ফর্ম আসবে:

   - **Description** (টোকেনের বর্ণনা): এখানে লিখুন → `superninja-deploy`
   
   - **Expiration** (মেয়াদ): এখানে একটা ডেট পিকার বা ড্রপডাউন থাকতে পারে। সম্ভব হলে একটা দূরের তারিখ সিলেক্ট করুন (যেমন ৯০ দিন বা তার বেশি পরের তারিখ)। যদি কোনো মেয়াদ অপশন না থাকে, তাহলে ফাঁকা রেখে দিন (কিছু অ্যাকাউন্টে মেয়াদ অপশনাল)।

৩. **"Generate token"** বাটনে ট্যাপ করুন। (সবুজ বা নীল রঙের বাটন।)

## ধাপ ৩: টোকেন কপি করুন (খুবই গুরুত্বপূর্ণ)

১. টোকেন তৈরি হওয়ার পর একটা বক্স আসবে যেখানে আপনার টোকেন লেখা থাকবে।

২. টোকেনটা **`nfp_`** দিয়ে শুরু হবে। যেমন: `nfp_abc123xyz456...`

৩. টোকেনের পাশে একটা **কপি আইকন** থাকবে। সেটায় ট্যাপ করে টোকেন কপি করুন।

   ⚠️ **সতর্কতা:** এই টোকেনটা শুধু একবারই দেখানো হবে! পেজ ছেড়ে গেলে আর দেখা যাবে না। এখনই কপি করে নিন।

৪. কপি করা টোকেনটা নোট করে রাখুন।

৫. **"Done"** বাটনে ট্যাপ করুন।

## ✅ Netlify টোকেন প্রস্তুত!

আপনার Netlify টোকেন প্রস্তুত। এটা `nfp_` দিয়ে শুরু হয়েছে। এটাও আমাকে মেসেজ করে দিন।

---

# অংশ ৪: আমাকে কী পাঠাবেন

আপনার কাজ শেষ হলে আমাকে এই ২টা টোকেন মেসেজ করে দিন:

### ১. Vercel Token (vcp_ দিয়ে শুরু)
```
vcp_আপনার_টোকেন_এখানে
```

### ২. Netlify Token (nfp_ দিয়ে শুরু)
```
nfp_আপনার_টোকেন_এখানে
```

আমি এই ২টা টোকেন পেলেই বাকি সব কাজ নিজে করবো:
- ✅ Vercel-এ কোড ডিপ্লয় করা (আপডেট করা)
- ✅ Netlify-এ কোড ডিপ্লয় করা (নতুন)
- ✅ Environment Variables সেট করা (JWT_SECRET, MONGODB_URI, NEXT_PUBLIC_SITE_URL)
- ✅ দুই প্ল্যাটফর্মেই সাইট লাইভ করা

---

# সারাংশ — যা যা মনে রাখবেন

| কিছু | বিস্তারিত |
|------|---------|
| **Vercel Token পেজ** | https://vercel.com/account/tokens |
| **Vercel Token প্রিফিক্স** | `vcp_` দিয়ে শুরু |
| **Vercel Scope** | "Full Account" সিলেক্ট করতে হবে |
| **Netlify সাইন আপ** | https://app.netlify.com → "Continue with GitHub" |
| **Netlify Token পেজ** | https://app.netlify.com/user/applications#personal-access-tokens |
| **Netlify Token প্রিফিক্স** | `nfp_` দিয়ে শুরু |
| **MongoDB URI** | ইতিমধ্যে আমাকে দেওয়া আছে, কাজ করছে ✅ |
| **Gemini API Key** | ডিপ্লয়ের পর আপনি Admin Panel থেকে লাগাবেন |
| **SMS Gateway API Key** | ডিপ্লয়ের পর আপনি Admin Panel থেকে লাগাবেন |
| **MongoDB মাল্টি-অ্যাকাউন্ট** | Admin Panel-এ "MongoDB Connections" সেকশন থেকে যতগুলো খুশি যোগ করতে পারবেন |

---

> **টোকেন তৈরি করার সময় কোনো সমস্যা হলে আমাকে বলুন। আমি সাহায্য করবো।**
> **টোকেন ২টা তৈরি হলে শুধু মেসেজ করে দিন। বাকি সব আমি করবো।**
